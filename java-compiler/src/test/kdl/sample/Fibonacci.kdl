type Fibonacci {
  run {
    // parse the first argument as an int
    int i: parseInt(args[0]);
    println(fib(i));
  }

  fnc int fib(int n) {
    if n <= 1 {
      return n;
    } else {
      return fib(n - 1) + fib(n - 2);
    }
  }

}
